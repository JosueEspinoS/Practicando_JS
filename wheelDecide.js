console.log('Wheel Decide')

var list = ["BM", "RED", "Blue", "Cobra", "Dragon"]

var results = {
    category: list[Math.floor(Math.random() * list.length)],
    //second: list[Math.floor(Math.random() * list.length)],
    //third: list[Math.floor(Math.random() * list.length)]

}

console.log(results);

//console.log(list[Math.floor(Math.random() * list.length)])

//console.log(Math.floor(Math.random() * list.length))